version: "3.8"

services:
  n8n:
    image: n8nio/n8n:1.50.2
    container_name: n8n
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    environment:
      - TZ=Asia/Ho_Chi_Minh
    restart: unless-stopped

volumes:
  n8n_data: